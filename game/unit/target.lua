require 'middleclass.lua'

Target = class('Target')

--[[ action types

attack
move

--]]

function Target:initialize(unit, tile, action)
  
end

function Target:hasMoved()
  if unit then
    
  else
    return nil
  end
end